// Bidders Angular 4 and Nodejs
How to INSTALL

DOWNLOAD FILES AND FOLDERS 
required npm install -g @angular/cli in command prompt
in your current folder in command prompt write 
npm install 
ng serve --open
